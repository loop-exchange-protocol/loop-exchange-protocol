{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://loop.exchange/schemas/v1alpha1/engine-config.schema.json",
  "title": "Loop Exchange EngineConfig",
  "type": "object",
  "additionalProperties": false,
  "required": ["api_version", "kind"],
  "properties": {
    "api_version": { "const": "loop.exchange/v1alpha1" },
    "kind": { "const": "EngineConfig" },
    "repositories": { "type": "array", "uniqueItems": true, "items": { "$ref": "#/$defs/repository" } },
    "bindings": { "type": "array", "uniqueItems": true, "items": { "$ref": "#/$defs/binding" } }
  },
  "$defs": {
    "identifier": { "type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]{0,127}$" },
    "namespace": { "type": "string", "pattern": "^(?:[a-z0-9](?:[a-z0-9-]{0,62}[a-z0-9])?\\.)+[a-z0-9](?:[a-z0-9-]{0,62}[a-z0-9])?$" },
    "digest": { "type": "string", "pattern": "^sha256:[0-9a-f]{64}$" },
    "contract": {
      "type": "object", "additionalProperties": false,
      "required": ["namespace", "name", "version"],
      "properties": {
        "namespace": { "$ref": "#/$defs/namespace" },
        "name": { "$ref": "#/$defs/identifier" },
        "version": { "$ref": "#/$defs/identifier" }
      }
    },
    "package": {
      "type": "object", "additionalProperties": false,
      "required": ["namespace", "name", "version"],
      "properties": {
        "namespace": { "$ref": "#/$defs/namespace" },
        "name": { "$ref": "#/$defs/identifier" },
        "version": { "$ref": "#/$defs/identifier" }
      }
    },
    "repository": {
      "type": "object", "additionalProperties": false,
      "required": ["id", "url"],
      "properties": {
        "id": { "$ref": "#/$defs/identifier" },
        "url": { "type": "string", "format": "uri", "pattern": "^(?:oci|https)://", "minLength": 1 },
        "credential": { "$ref": "#/$defs/identifier" },
        "auto_install": { "type": "boolean", "default": false },
        "trusted_namespaces": {
          "type": "array", "uniqueItems": true,
          "items": { "$ref": "#/$defs/namespace" }
        }
      },
      "allOf": [
        {
          "if": { "properties": { "auto_install": { "const": true } }, "required": ["auto_install"] },
          "then": { "required": ["trusted_namespaces"], "properties": { "trusted_namespaces": { "minItems": 1 } } }
        }
      ]
    },
    "implementation": {
      "type": "object", "additionalProperties": false,
      "required": ["source", "package"],
      "properties": {
        "source": { "enum": ["builtin", "helper", "repository"] },
        "package": { "$ref": "#/$defs/package" },
        "digest": { "$ref": "#/$defs/digest" },
        "command": {
          "type": "array", "minItems": 1,
          "items": { "type": "string", "minLength": 1 }
        }
      },
      "allOf": [
        { "if": { "properties": { "source": { "const": "builtin" } }, "required": ["source"] }, "then": { "not": { "anyOf": [{ "required": ["digest"] }, { "required": ["command"] }] } } },
        { "if": { "properties": { "source": { "const": "helper" } }, "required": ["source"] }, "then": { "required": ["command"], "not": { "required": ["digest"] } } },
        { "if": { "properties": { "source": { "const": "repository" } }, "required": ["source"] }, "then": { "required": ["digest"], "not": { "required": ["command"] } } }
      ]
    },
    "binding": {
      "type": "object", "additionalProperties": false,
      "required": ["kind", "contract", "implementation"],
      "properties": {
        "kind": { "enum": ["provider", "checker"] },
        "contract": { "$ref": "#/$defs/contract" },
        "implementation": { "$ref": "#/$defs/implementation" }
      }
    }
  }
}
